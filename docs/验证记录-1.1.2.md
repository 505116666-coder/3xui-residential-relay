# 1.1.2 验证记录

验证日期：2026-09-13。代码提交：7a7864cd7cd9c50bb8cac4722422b60717cfaf83。

- 90 项 Python 回归测试通过；生成脚本一致性、Bash 语法及 git diff 空白检查通过。
- 本机真实 Xray 回环集成通过：直连、认证 SOCKS、UDP、追加节点及上游故障时拒绝回退直连。
- 已有服务器安装校验通过，显示 relay 1.1.2 / 3X-UI v3.7.0；实际执行 relay update，完成固定提交下载、校验、备份和替换。
- 真实面板测试通过：临时节点仅监听 127.0.0.1，新增响应丢失回滚、正常新增、改名、替换上游、删除、最终清理。报告 ok=true、original_preserved=true。
- 原有三个节点的部署记录、路由和入站配置保持，三个节点 TCP 通过。基础住宅 UDP 未测通，追加住宅 UDP 通过。证书剩余至少 48 小时，续期 timer 正常。
- 真实 PTY 菜单验证通过：菜单等待时其他进程可获取操作锁；启动未改写 manager.py/relay，未重复提示安装命令；第 9 项更新可见；退出正常。

改名路径不再调用 systemctl 重启，仍允许面板自身重载；以上服务器本机检查不替代外部客户端公网入口验收。

服务器文件（保留在服务器，不公开凭据）：

- 管理器：/root/3xui-dual/manager.py
- 本次升级前备份：/root/3xui-dual/manager.before-1.1.2.py
- 更新命令上版备份：/root/3xui-dual/manager.previous.py
- 节点验证日志：/root/3xui-dual/live-validation-1.1.2.log
- 节点验证报告：/root/3xui-dual/live-validation-result.json
- 菜单验证报告：/root/3xui-dual/menu-validation-1.1.2.json

复测菜单可运行仓库 tests/live_menu_validation.py，仅在已部署服务器执行。
