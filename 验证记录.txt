验证日期：2026-09-11

已完成的本地验证：
1. Bash 单文件脚本：bash -n 通过。
2. Python 语法编译通过。
3. 16 项单元测试全部通过（含真实 PTY、连接域名/IPv4、自动检测及失败手动输入）：新版 x25519 密钥标签、默认阻断、路由顺序、UDP 阻断、住宅出口指向本机、特殊字符凭据、失败请求不重试直连、故障注入异常后的配置恢复、节点链接及文件权限。
4. 使用官方 Xray v26.7.28（提交 5ca6f4b，darwin/arm64）执行服务端 run -test，两个 VLESS/REALITY 入口和完整路由配置得到 Configuration OK。
5. 使用同一内核校验并启动两条临时测试客户端，两个 SOCKS5 监听仅绑定 127.0.0.1；测试后进程已退出。
6. 核对 3x-ui v3.7.0（f727d04f6522bb94a8fb52e8352fdcafb51c11e1）的 CLI、数据库 settings 表、CSRF 登录流程、入口 API、Xray 配置 API、流量统计规则与 HTTPS 配置方式。
7. 核对 Linux amd64/arm64 官方发行包 SHA-256；安装时再次校验。
8. 单文件 .sh 内嵌的 Python 内容与 deploy-3xui-dual.py 一致。

范围限制：
没有连接或修改用户的远程服务器；没有使用真实住宅代理凭据。
未在 Linux/systemd 环境完成整套远程安装，也未实际申请 IP 证书。
本地的故障恢复单元测试使用模拟 API/出口响应，不代表真实住宅链路已验收。
真实 VLESS/REALITY 握手、住宅出口、失败不回退、证书签发和续期任务检查由脚本在服务器上执行。
服务器本机协议验收后，还需从用户电脑/手机验证公网入口、客户端分流与 DNS。

脚本 SHA-256：
0a1231af9fa852f054f125e8636167838954f09d16ae2e6c88d1261077366958

修复记录：
2026-09-11：通过真实 PTY 复现 File or stream is not seekable；将 /dev/tty 的 r+ 句柄改为独立只读/只写句柄。普通输入及回车使用默认值均通过测试。
2026-09-11：住宅 SOCKS5 连接地址接受域名或公网 IPv4，去掉误导性“不是最终出口 IP”提示；服务器公网 IPv4 检测成功后直接使用，失败才手动输入。兼容旧 state.json 的 socks_ip 字段。域名测试使用合成域名和模拟 DNS，没有连接用户的真实代理。

2026-09-11：修复住宅节点元组缺少端口；端口随机且可自定义，冲突重试；取消轮换询问与出口相等要求。20项本地测试通过，含不同出口下故障恢复测试；Bash语法通过。未在用户Linux服务器实装验收。

2026-09-12 最新版本（取代旧版默认阻断/本机转发设计）：
26 项回归测试通过；Bash 语法与 Python 编译通过。
真实 Xray 26.7.28 服务端，分别搭配 26.7.28 和 25.5.16 客户端：REALITY 握手、认证 SOCKS TCP、UDP DNS、上游断线 TCP/UDP 不回退、直连仍可用均通过。25.5.16 测试同时验证无专属路由的新入站默认直连。测试仅用本机 TLS/HTTP/DNS 及模拟住宅服务，不代表用户真实住宅供应商支持 UDP。
计数服务已公开上线，GET 初始值 0、无效 POST 返回 400；数据库重复事件只计一次验证通过。没有伪造历史次数。
现有 Linux 面板迁移流程已做备份/回滚回归测试，尚未在用户服务器执行实际迁移。

2026-09-12 易用性更新：
37 项回归通过，包含 HTML 凭据转义、复制内容、计数失败不阻断、部分安装/完整安装/迁移资料/重复卸载/其他面板保留/仅移除标记防火墙规则。卸载使用临时目录和模拟服务测试，未操作真实 VPS。
计数服务 SQLite 验证：真实写入、重复编号去重、新编号递增、探针事务清理、异常输入与服务故障。线上 curl POST 两次成功，不计数诊断事件不增加成功安装数量。
结果页面已在内置浏览器用虚构账号检查，点击复制全部面板信息反馈成功；终端 OSC 52/OSC 8 能力依赖用户 SSH 客户端，提供离线结果网页。

2026-09-12 用户简化要求：删除 HTML 生成和下载流程；结果保留终端输出与 0600 文本备份。更新结果时删除旧 HTML。README 使用前准备采用 5 个编号标题、分点说明及指定收尾句。37 项回归通过。

2026-09-12：横幅适配终端宽度并加粗；删除安装次数上报和重试入口；隐藏结果保存/备份提示，保留私有文本备份。36 项回归通过，覆盖 10/20/40/80/120/200 列横幅、完成输出无网络请求、静默备份和原有路由/卸载检查。未在真实 VPS 上执行安装。

2026-09-12：按用户 DMIT 终端截图将单行字标改为 7 行高 DIDUSHAN 实心色块大字，蓝→青→蓝渐变，字形随终端宽度展开；窄终端分两排，极窄终端使用普通文字。8 种终端宽度和颜色降级等 5 项输出检查通过。

2026-09-12：新增 --add-residential：认证预检、独立端口/UUID/REALITY 密钥、原生 SOCKS 出站和 TCP/UDP 精确绑定、禁用入站先落路由再启用、真实协议本机检测、持久化恢复记录与 --rollback-add。保留原节点及自定义配置；结果和复制菜单支持追加节点。53 项回归通过；本机真实 Xray 验证新增节点 TCP/UDP、新上游故障不回退且原住宅及直连仍可用。没有在用户真实 VPS 上执行添加，云安全组需按提示放行。

2026-09-13：复现新增住宅 API 返回 settings 对象时重复 json.loads 的错误，且旧回滚流程同样报错。统一兼容对象/JSON 字符串，POST 表单中嵌套对象用 JSON 编码并略过 null 字段；覆盖新增、残留恢复、迁移和配置快照。对象格式场景修复前 3 项复现失败，修复后完整 61 项回归通过。未直接操作用户 VPS；旧失败残留需用新脚本 --rollback-add 后重试。

2026-09-13：再次新增在 update/:id 被拒绝；用 v3.7.0 同版本 Gin 1.12.0 和匹配的字段类型实际复现 clientStats 数组表单绑定失败（cannot unmarshal array into ClientTraffic）。新增改用官方 setEnable/:id，只提交 enable，并读取核对启用和节点参数；其他完整入站更新也排除只读 clientStats。64 项 Python 回归通过，独立 Go/Gin 参数绑定验证通过；这不是完整面板/VPS 端到端验收。截图中的失败已经成功自动回滚。

2026-09-13 管理优化（脚本 1.1.0，本地未发布）：
新增全节点 --check、统一 --menu、住宅上游替换/改名/追加节点删除、持久化 --rollback-change、脱敏 --diagnostics、curl 分类错误及独立脚本版本。单一 Python 源码通过 scripts/build.py 生成安装包和 SHA256SUMS，并同步视频工作目录副本。
79 项 Python 回归通过，包括修改失败恢复、删除响应丢失恢复、提交后残留记录、后续面板修改保护、多节点检查继续执行和诊断凭据排除。测试需要临时回环端口，在允许本地端口绑定的执行环境完成。
真实 Xray 本机测试通过：直连和住宅 TCP、住宅 UDP、新增节点、两个住宅上游分别故障时不回退且其他入口保持可用。仅使用虚构凭据和本机测试服务。
新增 tests/e2e_panel.py 完整 Linux 面板验收入口；只做了 Python 语法与命令帮助检查。当前 Mac 没有 Docker，也未提供一次性 Linux VPS，因此未执行真实面板/ACME/住宅供应商的端到端验收。未修改用户 VPS，未提交或推送 GitHub。

2026-09-13 15:06 +08:00 用户现有 VPS 实测：
经用户授权，通过当前 XTerminal SSH 会话操作。先备份 /root/3xui-dual 与 SQLite 数据库到 /root/relay-before-1.1.0-20260913-145145；未重装面板。
新版 Python 源码与安装包均完成 SHA256 比对；服务器 /root/deploy-3xui-dual.sh 及 /root/3xui-dual/manager.py 已更新为脚本 1.1.0。
现有 3 个节点真实 VLESS/REALITY 本机 TCP 出口检测均通过。基础住宅节点 UDP 暂未测通，原追加住宅节点 UDP 通过；证书剩余至少 48 小时、续期定时器和面板服务正常。
通过 tests/live_validation.py 创建仅监听 127.0.0.1 的临时住宅入站，使用服务器已保存的住宅代理凭据（未导出）。真实 API 新增响应丢失回滚、正常新增、改名、替换为另一已配置住宅上游、删除均通过。
测试后断言部署 state、原有全部入站和路由与测试前一致，并再次检查原节点连接。报告 ok=true、original_preserved=true；pending 记录为 0；临时节点清理完成。
实际运行 Shell 管理菜单、7 脱敏诊断、8 恢复入口（无中断操作）与 0 退出。SSH 已返回普通提示符。
曾尝试限时公钥连接，但服务器仅允许密码认证，未成功建立直连；临时公钥和本地私钥均已清理，未修改 sshd 配置。
服务器结果：/root/3xui-dual/live-validation-result.json；过程日志：/root/3xui-dual/live-validation.log；脱敏诊断：/root/3xui-dual/diagnostics.json。
本次验证针对已安装的实际面板和真实供应商链路；未在此服务器重新执行安装、卸载、迁移或重新申请证书，也未替代外部客户端的公网入口验收。未推送 GitHub。
